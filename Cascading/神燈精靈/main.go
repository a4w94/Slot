package main

import (
	"fmt"
	"package/src/table"
)

func main() {
	// foo := graph.AddShareMemory(10)
	// fmt.Println(len(foo))
	// fmt.Println(foo)
	// fmt.Println("Test1")
	// scoretools.Test()
	table.Init_Cascading()
	fmt.Println("Test2")

	// rand.Seed(int64(time.Now().UnixNano()))
	// table.InitTable()
	// public.ChangeGlobalRtpModule_Cascading(965)
	// var tmp tools.Cascading_Rng
	// tmp.Generate_Rng(command.MainGame)
	//calculate.Simulate(1, 965)
	//calculate.PlayerSim(100000, 965)

	//製圖
	//chart.WriteDataChart()
	// for a := 1; a < 6; a++ {
	// 	for b := 0; b < 6; b++ {
	// 		for c := 0; c < 6; c++ {
	// 			fmt.Println(a, ",", b, ",", c)

	// 		}
	// 	}
	// }

}
